import { StyleSheet } from 'react-native';
import { screenBackgroundColor } from '..';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: screenBackgroundColor,
  },
});
